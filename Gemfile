# frozen_string_literal: true

source "https://rubygems.org"

# Specify your gem's dependencies in magicprotorb.gemspec
gemspec

gem "minitest", "~> 5.16"
gem "rake", "~> 13.0"
gem "rubocop", "~> 1.21"

# Compiles the Rust extension during development (`rake compile`).
gem "rake-compiler", "~> 1.2"

# Used by the gRPC synthesis tests; optional at runtime.
gem "grpc", "~> 1.6"
